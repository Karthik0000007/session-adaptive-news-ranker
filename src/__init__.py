# Session-Adaptive News Ranker
# Production-grade multi-objective news feed optimization system
#
# Lazy imports: individual modules are imported directly to avoid
# cascading import failures when optional dependencies are missing.
# Use: `from src.drift_detector import DriftDetector` instead of `from src import DriftDetector`

__version__ = "2.0.0"
